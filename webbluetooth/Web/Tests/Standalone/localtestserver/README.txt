Starts the local websocket server needed to run for the local tests.

To run the server:

1) Open a command prompt in this folder and run the command 'npm install' to install dependencies
2) To start the server, run command 'npm run serve' in a command prompt in this folder
3) With the server running, open a new commandline window at the brach root and run 'grunt local' to execute the tests