define (videf1 dv(.VirtualInstrument (
    c(
        e(dv(.Boolean false)  fp1)   // stop 3
        e(dv(.Boolean false)  fp2)   // stop 2
    ) 
    clump(1 
       Perch(0)
    )
)))
enqueue (videf1)
