// Autogenerated Vireo assembly file
// Generated on Tue Dec 03 10:54:41 2013
// This tests passing a * to a subvi call which indicates that the subVI should use the default value for that parameter.

define (c0 dv(.Int32 5) )
define (c1 dv(.Int32 1) )

//---------------------------------------------------
//VI Definition: simple_subVI_defaultdata_input.vi
define (videf2 dv(.VirtualInstrument (
    c(
        o(dv(.Int32 0) local4)   // result
        i(dv(.Int32 42) local1)   // b
        i(dv(.Int32 0) local2)   // a
    )
    c(
        e(.Int32 local3)
        e(dv(.Int32 -1) local5)
    )
    clump(1 
        Add(local2 local1 local3)
        Copy(local3 local4 )                      // FP Term
        DPrintf("In SubVI with optional parameters %2s + %2s => %2s" local1 local2 local4)
//--- end of vi
    )
)))

//---------------------------------------------------
//VI Definition: Untitled 1
define (videf1 dv(.VirtualInstrument (
    c(
        e(.Int32 local1)   // result
    )
    clump(1 
        videf2(local1 c0 c1)  //call the subvi and overwrite the inputs with constant data
        Print(local1)        //6            
        videf2(local1 * c1) //call the subvi again and pass a * as input, subvi call should re-init the input to its default.
        Print(local1) //43
        videf2(local1 c1 *) //call the subvi again and pass a * as input, subvi call should re-init the input to its default.
        Print(local1) //1

        videf2(* * *) //call the subvi again and pass a * as output, which should be interpreted as a discarded output.
//--- end of vi
    )
)))

enqueue (videf1)
