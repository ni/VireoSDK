define(['Vireo'], function (Vireo) {
    'use strict';

    describe('just checking', function () {
        it('works for app', function () {
            expect(typeof Vireo).toBe('function');
        });
    });
});
