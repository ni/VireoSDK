define(MyVI dv(VirtualInstrument (
    Locals: c(
        e(dv(.UInt32 0) handle)
        e('' url)
        e('' outputFile)
        e('' headers)
        e('' body)
        e(c(
            e(.Boolean status)
            e(.Int32 code)
            e(.String source)
        ) error)
    )
    clump (
        // handle(0), url(1), output file(2), timeout(3), headers(4), body(5), error(6), ocurrence(7)
        HttpClientGet(handle url outputFile * headers body error)
    )
) ) )

enqueue(MyVI)