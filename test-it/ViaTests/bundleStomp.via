// Autogenerated Vireo assembly file
// Generated on Wed Nov 20 14:45:03 2013

define(c0 dv(.Single 1) )
//---------------------------------------------------
//VI Definition: floating_point_bundlestomp3.vi
define(videf1 dv(.VirtualInstrument (
    c(
        o(dv(.Single 9) local7)   // Numeric 2
        i(dv(c(
            e(.Single n0)   // Numeric
            e(.Single n1)   // Numeric 2
            e(.Single n2)   // Numeric 3
        ) (-5 1 8 )) local1)   // Cluster
    )
    c(
        e(.Single local2)   // Numeric
        e(.Single local3)   // Numeric 2
        e(.Single local4)   // Numeric 3
        e(.Single local5)
        e(c(
            e(.Single n0)
            e(.Single n1)
            e(.Single n2)
        ) local6)
        e(c(
            e(.Single n0)   // Numeric
            e(.Single n1)   // Numeric 2
            e(.Single n2)   // Numeric 3
        ) local8)   // Cluster
        e(c(
            e(.Single n0)   // Numeric
            e(.Single n1)   // Numeric 2
            e(.Single n2)   // Numeric 3
        ) local9)
    )
    clump(1 
        Copy(local1.n0 local2 )                   // unbundle
        Copy(local1.n1 local3 )                   // unbundle
        Copy(local1.n2 local4 )                   // unbundle
        Add(local4 c0 local5)
        Convert(local5 local6)
        Copy(local5 local7 )                      // FP Term
        Add(local3 c0 local4)
        Add(local2 c0 local3)
        Copy(local1 local8 )
        Copy(local3 local8.n0 )                   // bundle
        Copy(local4 local8.n1 )                   // bundle
        Copy(local5 local8.n2 )                   // bundle
        Add(local8 local6 local9)
        Println(local9)
//--- end of vi
    )
)))
enqueue(videf1)
