// Autogenerated Vireo assembly file
// Generated on Tue Dec 03 10:54:41 2013
// This tests passing a * to a subvi call which indicates that the subVI should use the default value for that parameter.

define(c0 dv(.Int32 5) )
define(c1 dv(.Int32 1) )

//---------------------------------------------------
define(videf2 dv(.VirtualInstrument (
    c(
        o(dv(.Int32 0) local4)   // result
        i(dv(.Int32 42) local1)   // b
        i(dv(.Int32 0) local2)   // a
    )
    c(
        e(.Int32 local3)
        e(dv(.Int32 -1) local5)
        e(dv(a(String *) ()) callchain)
    )
    clump(1
        CallChain(callchain)
        Printf("videf2 Callchain: %z\n" callchain)
        Trigger(1)
        Trigger(2)
        Wait(1)
        Wait(2)
//--- end of vi
    )
    clump(1
        CallChain(callchain)
        Printf("videf2 Clump1 Callchain: %z\n" callchain)
        videf3()
        Trigger(3)
        Wait(3)
    )
    clump(1
        videf3()
        CallChain(callchain)
        Printf("videf2 Clump2 Callchain: %z\n" callchain)
    )
    clump(1
        CallChain(callchain)
        Printf("videf2 Clump3 Callchain: %z\n" callchain)
    )
)))

//---------------------------------------------------
define(videf3 dv(.ReentrantVirtualInstrument (
    c(
        e(dv(a(String *) ()) callchain)
    )
    clump(1
        CallChain(callchain)
        Printf("videf3 Callchain: %z\n" callchain)
//--- end of vi
    )
)))

//---------------------------------------------------
define(videf dv(.VirtualInstrument (
    c(
        e(.Int32 local1)   // result
        e(dv(a(String *) ()) callchain)
    )
    clump(1
        CallChain(callchain)
        Printf("videf Callchain: %z\n" callchain)
        videf2(local1 c0 c1)  //call the subvi and overwrite the inputs with constant data
        CallChain(callchain)
        Printf("videf Callchain: %z\n" callchain)
        videf3()
//--- end of vi
    )
)))

enqueue(videf)
